// /app/recap/page.js
import RecapClient from "./RecapClient";

export default function RecapPage() {
  // Super simple server component that just renders the client component
  return <RecapClient />;
}
